<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MUSE_Android">

        <activity android:name=".search.SearchResultActivity" android:exported="false"/>
        <activity android:name=".search.SearchActivity" android:exported="false"/>

        <activity
            android:name=".profile.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".notification.NotificationScreenActivity"
            android:exported="false" />
        <activity
            android:name=".policy.PrivacyPolicyActivity"
            android:exported="false" />
        <activity
            android:name=".policy.TermsOfUseActivity"
            android:exported="false" />

        <activity
            android:name=".product.ProductDetailActivity"
            android:exported="false" />
        <activity
            android:name=".product.CategoryProductsActivity"
            android:exported="false" />

        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
