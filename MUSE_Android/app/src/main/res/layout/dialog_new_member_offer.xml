<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_new_member_bottom_sheet"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="20dp">

    <!-- Handle Bar -->
    <View
        android:id="@+id/bottomSheetHandle"
        android:layout_width="48dp"
        android:layout_height="4dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/bg_bottom_sheet_handle" />

    <!-- Chibi character image holding a voucher -->
    <ImageView
        android:id="@+id/ivOfferImage"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginBottom="16dp"
        android:src="@drawable/png_bottom_dialog"
        android:scaleType="fitCenter"
        android:contentDescription="New Member Offer Chibi Character" />

    <!-- Heading Title -->
    <TextView
        android:id="@+id/tvOfferTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:fontFamily="@font/montserrat"
        android:gravity="center"
        android:text="Ưu đãi dành cho thành viên mới đang chờ đón bạn"
        android:textColor="#24191A"
        android:textSize="20sp"
        android:textStyle="bold" />

    <!-- Subtitle/Description -->
    <TextView
        android:id="@+id/tvOfferDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:fontFamily="@font/montserrat"
        android:gravity="center"
        android:text="Nhận phiếu giảm giá 100.000 VNĐ cho đơn hàng tiếp theo trị giá 1.000.000 VNĐ khi liên kết tài khoản MUSE.com."
        android:textColor="#574145"
        android:textSize="14sp" />

    <!-- Primary Action Button: LOGIN -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnOfferLogin"
        style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginBottom="12dp"
        android:backgroundTint="#24191A"
        android:fontFamily="@font/montserrat"
        android:text="ĐĂNG NHẬP ĐỂ NHẬN PHIẾU GIẢM GIÁ"
        android:textColor="#FFF8F7"
        android:textSize="13sp"
        android:textStyle="bold"
        android:letterSpacing="0.1"
        app:cornerRadius="4dp" />

    <!-- Secondary Action Button: SKIP -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnOfferSkip"
        style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginBottom="20dp"
        android:backgroundTint="@android:color/transparent"
        android:fontFamily="@font/montserrat"
        android:text="BỎ QUA"
        android:textColor="#24191A"
        android:textSize="13sp"
        android:textStyle="bold"
        android:letterSpacing="0.1"
        app:strokeColor="#24191A"
        app:strokeWidth="1dp"
        app:cornerRadius="4dp" />

    <!-- Checkbox: Do not show again -->
    <CheckBox
        android:id="@+id/cbDontShowAgain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:buttonTint="#DDBFC3"
        android:fontFamily="@font/montserrat"
        android:text="Không hiển thị lại thông báo này"
        android:textColor="#574145"
        android:textSize="14sp" />

</LinearLayout>
